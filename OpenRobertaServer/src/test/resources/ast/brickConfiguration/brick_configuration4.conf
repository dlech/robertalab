ev3 craesy {
  wheel diameter 5.5 cm
  track width    17.0 cm
    sensor port 1 touch
    sensor port 2 color
    sensor port 3 ultrasonic
    sensor port 4 touch
    actor port A middle motor { regulated foreward }
    actor port B large motor { regulated foreward }
    actor port C large motor { unregulated backward left }
    actor port D large motor { regulated foreward right }
}